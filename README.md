# webserv
vois si on conserve log en class ou si on la passe en namespace en supprimant _level qui gere le niveau d'affichage des infos du projet.

continuer les fonctions Utils

Utilitee d'utiliser common.hpp ? la norme est que chaque ficher doit posseder tous les include qui lui sont necessaire pas de gros paquets d'include qui ralentissent la compilation
