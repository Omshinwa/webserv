*This project has been created as part of the 42 curriculum by dasamuel, wiwu.*

# features

Run ./webserver [CONFIG]

A HTTP/1.0 webserver.
An Event listener and dispatcher (to event handlers).
An `event` is a file descriptor being readable or writable.