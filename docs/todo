# Mandatory
* cookies
* handle multiple server config with different host:port

* add CGI stuff to poll() * the fd will need to sit in the poll(), also Connexion needs to have a cgi_pid ? idk

# Maybe
* handle all the vars of CGI
* 1.1 HTTP? chunked 

* Prettier logger (add the timestamp), make debug() info() more consistent
* draw a schema of the whole control flow

# checks to do when we're done

* config's body size limit is exceeded
* do stress tests (idk how, basically have tons of connexions to the server)
* timeout the cgi if it's too long
* tests all the errors, 431 etc

http://localhost:8080/cgi-bin/cgi_tester