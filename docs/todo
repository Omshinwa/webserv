# BIG TODO
* cookies
* to solidify my understanding of Host, i'd like to have several server configs

# CGI
* CGI understanding / starts it
* config will eventually need to have map of extension -> interpreter
location /cgi-bin/ {
    methods GET POST;
    root ./www;
    cgi .py /usr/bin/python3;
    cgi .php /usr/bin/php-cgi;
}
* for now we just execute it with a shebang so the shell finds the interpreter for us
* the fd will need to sit in the poll(), also Connexion needs to have a cgi_pid ? idk

# checks to do

* config's body size limit is exceeded
* do stress tests (idk how, basically have tons of connexions to the server)
* timeout the cgi if it's too long
* tests all the errors, 431 etc
* 1.1 HTTP? chunked 
* handle all the vars of CGI
* add CGI stuff to poll()

http://localhost:8080/cgi-bin/cgi_tester