{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch prog",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/webserv",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "make",
            "osx": {
                "MIMode": "lldb"
            },
            "linux": {
                "MIMode": "gdb",
                "miDebuggerPath": "/usr/bin/gdb"
            }
        }
    ]
}
